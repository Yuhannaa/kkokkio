<%= form_for(current_user.favorites.build(post_id: post.id), remote: true) do |f| %>
  <div><%= f.hidden_field :post_id %></div>
  <%= f.submit "Favorite", class: "btn btn-primary favorite-btn" %>
<% end %>